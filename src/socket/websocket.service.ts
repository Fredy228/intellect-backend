// import { Adapter } from 'socket.io-adapter';

export class WebsocketService {
  // getClientRooms(adapter: Adapter): string[] {
  //   const { rooms } = adapter;
  //
  //   return Object.keys(rooms);
  // }
}
